import { Validation } from '../../protocols/validation';
export declare class UrlValidor implements Validation {
    private readonly _url;
    constructor(url: string);
    validate(): Error | null;
    private _isNotionPargeUrl;
}
