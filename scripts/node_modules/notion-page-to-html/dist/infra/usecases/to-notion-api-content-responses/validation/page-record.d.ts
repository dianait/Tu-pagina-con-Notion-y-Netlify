import { Validation } from '../../../protocols/validation';
import { HttpResponse } from 'data/protocols/http-request';
export declare class PageRecordValidator implements Validation {
    private readonly _notionPageId;
    private readonly _pageRecord;
    constructor(notionPageId: string, pageRecord: HttpResponse);
    validate(): Error | null;
}
