import { Validation } from '../../../protocols/validation';
export declare class NotionPageIdValidator implements Validation {
    private readonly _notionPageId;
    constructor(notionPageId: string | undefined);
    validate(): Error | null;
}
